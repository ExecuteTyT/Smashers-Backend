# Руководство по настройке базы данных (Senior Developer Approach)

## Правильный порядок действий

Как senior developer, мы следуем принципу: **сначала структура, потом данные**.

### ✅ Правильный порядок:

1. **Создать схему БД (таблицы)** - через Prisma миграции
2. **Проверить структуру** - убедиться что все таблицы созданы
3. **Запустить парсер** - заполнить таблицы данными из Django админки

### ❌ Неправильный порядок:

- Парсить данные до создания таблиц (приведет к ошибкам)
- Создавать таблицы вручную через SQL (потеряем версионирование)

---

## Вариант 1: Применение миграций на сервере (рекомендуется)

Если проект уже на сервере или вы можете загрузить его:

### Шаг 1: Загрузить проект на сервер

```bash
# На сервере
cd /opt
git clone <your-repo-url> smashers-backend
# или загрузить через scp/rsync
cd smashers-backend
```

### Шаг 2: Настроить .env

```bash
# Создать .env
cp .env.example .env
nano .env
```

Добавить:
```env
DATABASE_URL=postgresql://smashers:ваш_пароль@localhost:5432/smashers_db
```

### Шаг 3: Применить миграции

```bash
# Установить зависимости
npm install

# Сгенерировать Prisma Client
npx prisma generate

# Применить схему к БД (создаст все таблицы)
npx prisma db push
```

### Шаг 4: Проверить таблицы

```bash
# Через Prisma Studio (GUI)
npm run studio
# Откроется на http://localhost:5555

# Или через psql
docker exec -it smashers-postgres psql -U smashers -d smashers_db -c "\dt"
```

### Шаг 5: Запустить парсер

```bash
# Заполнить таблицы данными из Django админки
npm run parse
```

---

## Вариант 2: Применение миграций с локальной машины

Если проект на вашем компьютере, но БД на удаленном сервере:

### Шаг 1: Настроить .env локально

```bash
# В проекте smashers-backend
cp .env.example .env
nano .env
```

Добавить connection string к удаленной БД:
```env
DATABASE_URL=postgresql://smashers:пароль@IP_СЕРВЕРА:5432/smashers_db
```

**Важно:** Если БД доступна только локально на сервере, используйте SSH туннель:

```bash
# Создать SSH туннель (в отдельном терминале)
ssh -L 5433:localhost:5432 root@IP_СЕРВЕРА

# В .env использовать:
DATABASE_URL=postgresql://smashers:пароль@localhost:5433/smashers_db
```

### Шаг 2: Применить миграции

```bash
cd smashers-backend

# Установить зависимости
npm install

# Сгенерировать Prisma Client
npx prisma generate

# Применить схему
npx prisma db push
```

### Шаг 3: Проверить

```bash
# Через Prisma Studio
npm run studio

# Или через скрипт проверки
node -e "const { PrismaClient } = require('@prisma/client'); const p = new PrismaClient(); p.\$queryRaw\`SELECT table_name FROM information_schema.tables WHERE table_schema = 'public'\`.then(r => { console.log(r); p.\$disconnect(); });"
```

---

## Вариант 3: Использование готового скрипта

Мы создали скрипт для автоматизации:

### На сервере:

```bash
cd /opt/smashers-backend

# Сделать скрипт исполняемым
chmod +x scripts/apply-migrations-remote.sh

# Запустить
./scripts/apply-migrations-remote.sh
```

### С локальной машины:

```bash
cd smashers-backend

# Настроить .env с DATABASE_URL к удаленной БД
# Затем запустить:
node scripts/apply-migrations-remote.js
```

---

## Структура создаваемых таблиц

После применения миграций будут созданы следующие таблицы:

1. **categories** - Категории тренировок
2. **memberships** - Абонементы
3. **locations** - Локации для тренировок
4. **sessions** - Тренировки/занятия
5. **booking_requests** - Заявки с сайта
6. **sync_status** - Статус синхронизации

### Проверка структуры:

```sql
-- Посмотреть все таблицы
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
ORDER BY table_name;

-- Посмотреть структуру конкретной таблицы
\d sessions
```

---

## После создания таблиц

### 1. Запустить парсер для заполнения данными

```bash
npm run parse
```

Парсер:
- Подключится к Django админке
- Спарсит категории, локации, абонементы, тренировки
- Сохранит все в PostgreSQL через `dbSync.service.js`

### 2. Проверить данные

```bash
# Через Prisma Studio
npm run studio

# Или через psql
docker exec -it smashers-postgres psql -U smashers -d smashers_db

# Проверить количество записей
SELECT 
  (SELECT COUNT(*) FROM categories) as categories,
  (SELECT COUNT(*) FROM memberships) as memberships,
  (SELECT COUNT(*) FROM locations) as locations,
  (SELECT COUNT(*) FROM sessions) as sessions;
```

---

## Troubleshooting

### Ошибка "relation does not exist"

Таблицы еще не созданы. Примените миграции:
```bash
npx prisma db push
```

### Ошибка подключения к БД

1. Проверьте `DATABASE_URL` в `.env`
2. Проверьте, что PostgreSQL запущен: `docker ps | grep postgres`
3. Проверьте доступность порта: `telnet localhost 5432`

### Ошибка "P1001: Can't reach database server"

1. Проверьте firewall на сервере
2. Для удаленного доступа используйте SSH туннель
3. Проверьте, что PostgreSQL слушает на нужном интерфейсе

### Ошибка при применении миграций

Если есть конфликты:
```bash
# Применить с принудительным обновлением (⚠️ потеря данных!)
npx prisma db push --force-reset

# Или создать миграцию вручную
npx prisma migrate dev --name init
```

---

## Production Best Practices

### 1. Использовать миграции вместо db push

В production используйте версионированные миграции:

```bash
# Создать миграцию
npx prisma migrate dev --name init

# Применить в production
npx prisma migrate deploy
```

### 2. Резервное копирование перед миграциями

```bash
# Создать бэкап
docker exec smashers-postgres pg_dump -U smashers smashers_db > backup_before_migration.sql
```

### 3. Тестирование на staging

Всегда тестируйте миграции на staging окружении перед production.

---

## Чек-лист

- [ ] PostgreSQL запущен и доступен
- [ ] `.env` файл настроен с правильным `DATABASE_URL`
- [ ] Зависимости установлены: `npm install`
- [ ] Prisma Client сгенерирован: `npx prisma generate`
- [ ] Схема применена: `npx prisma db push`
- [ ] Таблицы созданы (проверено через `\dt` или Prisma Studio)
- [ ] Парсер запущен: `npm run parse`
- [ ] Данные заполнены (проверено через Prisma Studio)

---

## Следующие шаги

После успешного создания таблиц и заполнения данными:

1. ✅ База данных готова
2. ⏭️ Настроить cron job для автоматического парсинга
3. ⏭️ Запустить API сервер
4. ⏭️ Протестировать API endpoints

Готово! 🎉

# Анализ API эндпоинтов для фронтенда

## Текущие эндпоинты (уже реализованы)

### ✅ Health & Status
- `GET /api/health` - базовая проверка здоровья
- `GET /api/health/detailed` - детальная проверка
- `GET /api/sync-status` - статус синхронизации
- `GET /api/stats` - статистика БД

### ✅ Categories (Категории)
- `GET /api/categories` - все видимые категории (`isVisible: true`)
- `GET /api/categories/:id` - категория по ID (включает связанные сессии)
- `GET /api/categories/all` - все категории (админ, требует API ключ)

**Фильтрация:** Только видимые (`isVisible: true`)

### ✅ Memberships (Абонементы)
- `GET /api/memberships` - все видимые абонементы (`isVisible: true`)
- `GET /api/memberships/:id` - абонемент по ID (включая невидимые)
- `GET /api/memberships/by-type/:type` - абонементы по типу (только видимые)
- `GET /api/memberships/all` - все абонементы (админ, требует API ключ)

**Важно:** 
- Для разовых посещений использовать `/api/memberships/2` (id=2, цена 1200 руб)
- Абонемент с id=2 может быть невидимым, но доступен по прямому запросу

**Фильтрация:** Только видимые (`isVisible: true`)

### ✅ Sessions (Тренировки)
- `GET /api/sessions` - тренировки с фильтрами
  - Параметры: `date`, `date_from`, `date_to`, `category_id`, `location_id`, `available_only`, `limit`, `offset`
  - Возвращает только активные (`status: 'Активно'`)
- `GET /api/sessions/:id` - тренировка по ID (включает category и location)
- `GET /api/sessions/upcoming` - предстоящие тренировки (только с доступными местами)
- `GET /api/sessions/by-date/:date` - тренировки на конкретную дату
- `GET /api/sessions/all` - все тренировки (админ, требует API ключ)

**Фильтрация:** Только активные (`status: 'Активно'`)

### ✅ Locations (Локации)
- `GET /api/locations` - все видимые локации (`showLocation: true` И `showOnBookingScreen: true`)
- `GET /api/locations/:id` - локация по ID (включает связанные сессии)
- `GET /api/locations/:id/sessions` - тренировки в локации
  - Параметры: `limit`, `date_from`, `date_to`
  - Возвращает только активные и будущие тренировки
- `GET /api/locations/all` - все локации (админ, требует API ключ)

**Фильтрация:** Только видимые для экрана записи (`showLocation: true` И `showOnBookingScreen: true`)

### ✅ Booking (Заявки)
- `POST /api/booking` - создать заявку (публичный)
- `GET /api/booking` - все заявки (админ, требует API ключ)
- `GET /api/booking/:id` - заявка по ID (админ, требует API ключ)
- `POST /api/booking/:id/resend` - переотправить в Telegram (админ, требует API ключ)

## Возможные пробелы и рекомендации

### 🔍 Анализ структуры данных

Из Prisma схемы видно:
- **Category** - имеет `isVisible`, `sortOrder`
- **Membership** - имеет `isVisible`, `type`, `price`, `sessionCount`
- **Session** - имеет `status`, `datetime`, `locationId`, `categoryId`, `maxSpots`, `availableSpots`, `trainers`, `name`
- **Location** - имеет `showLocation`, `showOnBookingScreen`, `description`, `sortOrder`

### ✅ Что уже хорошо покрыто

1. **Базовые CRUD операции** - все сущности имеют получение списка и по ID
2. **Фильтрация по видимости** - правильно реализована для всех сущностей
3. **Связанные данные** - сессии включают category и location, категории включают сессии
4. **Фильтрация тренировок** - по дате, категории, локации, доступности
5. **Специальные случаи** - разовая тренировка (id=2), предстоящие тренировки

### 🤔 Потенциальные улучшения (если понадобятся)

#### 1. Категории
- ✅ `GET /api/categories/:id/sessions` - **НЕ РЕАЛИЗОВАНО**, но можно использовать `/api/sessions?category_id=:id`
- ✅ `GET /api/categories` - уже возвращает видимые, этого достаточно

**Рекомендация:** Использовать `/api/sessions?category_id=:id` для получения тренировок по категории

#### 2. Абонементы
- ✅ `GET /api/memberships/by-type/:type` - уже есть
- ✅ Получение по ID - уже есть, работает для невидимых

**Рекомендация:** Все необходимое есть

#### 3. Тренировки
- ✅ Фильтрация по дате - есть (`date`, `date_from`, `date_to`)
- ✅ Фильтрация по категории - есть (`category_id`)
- ✅ Фильтрация по локации - есть (`location_id`)
- ✅ Фильтрация по доступности - есть (`available_only`)
- ✅ Предстоящие тренировки - есть (`/api/sessions/upcoming`)
- ✅ Тренировки на дату - есть (`/api/sessions/by-date/:date`)

**Рекомендация:** Все необходимое есть

#### 4. Локации
- ✅ Получение видимых - есть
- ✅ Тренировки в локации - есть (`/api/locations/:id/sessions`)
- ✅ Фильтрация тренировок по дате - есть

**Рекомендация:** Все необходимое есть

#### 5. Комбинированные запросы
- ❓ `GET /api/sessions?category_id=1&location_id=2&date=2024-01-29` - **РЕАЛИЗОВАНО** ✅
- ❓ `GET /api/sessions?available_only=true&date_from=2024-01-01` - **РЕАЛИЗОВАНО** ✅

**Рекомендация:** Все комбинации фильтров работают

### 📋 Чек-лист для фронтенда

#### Обязательные эндпоинты (для базового функционала):
- [x] `GET /api/categories` - список категорий для фильтров
- [x] `GET /api/memberships` - список абонементов для каталога
- [x] `GET /api/memberships/2` - разовая тренировка (цена 1200 руб)
- [x] `GET /api/locations` - список локаций для фильтров
- [x] `GET /api/sessions` - список тренировок с фильтрами
- [x] `GET /api/sessions/upcoming` - предстоящие тренировки
- [x] `GET /api/sessions/:id` - детали тренировки
- [x] `GET /api/locations/:id/sessions` - тренировки в локации
- [x] `POST /api/booking` - отправка заявки

#### Опциональные эндпоинты (для расширенного функционала):
- [ ] `GET /api/categories/:id` - детали категории (если нужны связанные сессии)
- [ ] `GET /api/memberships/:id` - детали абонемента
- [ ] `GET /api/locations/:id` - детали локации (если нужны связанные сессии)
- [ ] `GET /api/sessions/by-date/:date` - альтернатива `?date=:date`
- [ ] `GET /api/memberships/by-type/:type` - фильтрация абонементов по типу

## Выводы

### ✅ Все необходимое для фронтенда уже реализовано

1. **Получение данных:**
   - ✅ Категории (только видимые)
   - ✅ Абонементы (только видимые + специальный id=2)
   - ✅ Локации (только видимые для записи)
   - ✅ Тренировки (с множеством фильтров)

2. **Фильтрация:**
   - ✅ По видимости (автоматически)
   - ✅ По дате (одиночная, диапазон)
   - ✅ По категории
   - ✅ По локации
   - ✅ По доступности мест
   - ✅ Комбинации фильтров

3. **Специальные случаи:**
   - ✅ Разовая тренировка (id=2)
   - ✅ Предстоящие тренировки
   - ✅ Тренировки в локации

4. **Отправка данных:**
   - ✅ Создание заявки

### 🎯 Рекомендации

**Для базового функционала сайта достаточно:**
- `GET /api/categories`
- `GET /api/memberships` + `GET /api/memberships/2`
- `GET /api/locations`
- `GET /api/sessions` (с фильтрами)
- `GET /api/sessions/upcoming`
- `POST /api/booking`

**Дополнительные эндпоинты можно использовать для:**
- Детальных страниц (по ID)
- Расширенной фильтрации
- Административных функций

### ✨ Итог

**API полностью готово для фронтенда!** Все необходимые эндпоинты реализованы, фильтрация работает корректно, специальные случаи (разовая тренировка) обработаны. Дополнительные эндпоинты не требуются для базового функционала сайта.
